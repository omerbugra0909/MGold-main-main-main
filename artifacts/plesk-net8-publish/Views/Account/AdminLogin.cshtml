@model MGold.Controllers.AdminLoginViewModel
@{
    ViewData["Title"] = "Yönetim Girişi";
    ViewData["PageDescription"] = "Sistem yöneticisi ve firma yöneticisi hesapları için güvenli panel girişi.";
    var returnUrl = ViewData["ReturnUrl"]?.ToString();
}

@section Styles {
    <style>
        .admin-auth {
            width: min(880px, 100%);
            margin: 0 auto;
            padding: 0;
            display: grid;
            grid-template-columns: .92fr 1.08fr;
            overflow: hidden;
            background: linear-gradient(135deg, rgba(17,19,25,0.96), rgba(43,31,18,0.94));
        }
        .auth-side {
            padding: 32px;
            background: linear-gradient(180deg, rgba(227,201,145,0.12), rgba(255,255,255,0.03));
            border-right: 1px solid rgba(255,214,116,0.1);
            display: grid;
            align-content: space-between;
            gap: 26px;
        }
        .auth-side h2 {
            margin: 0 0 10px;
            font-family: "Cinzel", serif;
            font-size: clamp(34px, 5vw, 48px);
            color: var(--gold-0);
            line-height: 1;
        }
        .auth-side p { color: var(--muted); line-height: 1.7; margin: 0; }
        .auth-form {
            display: grid;
            gap: 14px;
            padding: 32px;
        }
        .auth-form h3 {
            margin: 0;
            font-family: "Space Grotesk", sans-serif;
            font-size: 24px;
            color: var(--ink);
        }
        .admin-note {
            padding: 14px 16px;
            border-radius: 16px;
            background: rgba(255,214,116,0.08);
            border: 1px solid rgba(255,214,116,0.12);
            color: var(--muted);
        }
        .login-shortcuts {
            display: grid;
            gap: 10px;
            color: var(--muted);
            font-size: 13px;
        }
        @@media (max-width: 760px) {
            .admin-auth { grid-template-columns: 1fr; }
            .auth-side { border-right: 0; border-bottom: 1px solid rgba(255,214,116,0.1); }
        }
    </style>
}

<section class="surface admin-auth">
    <div class="auth-side">
        <div>
            <span class="badge">Yönetim Paneli</span>
            <h2>Yönetim Girişi</h2>
            <p>Sistem yöneticisi platform genelini, firma yöneticisi ise yalnızca kendi firmasına ait operasyonları yönetir. Personel ve müşteri girişleri ayrı akışlarla korunur.</p>
        </div>
        <div class="login-shortcuts">
            <div>Yetkisiz erişim denemeleri kayıt altına alınır.</div>
            <div>Hesap bilgileriniz yalnızca güvenli kimlik doğrulama ekranlarında kullanılmalıdır.</div>
        </div>
    </div>

    <form method="post" class="auth-form">
        @Html.AntiForgeryToken()
        <input type="hidden" name="returnUrl" value="@returnUrl" />
        <h3>Panel oturumu aç</h3>
        @Html.ValidationSummary(false)
        <label>
            <span>E-posta veya kullanıcı adı</span>
            <input asp-for="Username" />
        </label>
        <label>
            <span>Şifre</span>
            <input asp-for="Password" type="password" />
        </label>
        <button type="submit" class="btn btn-primary">Giriş Yap</button>
        <div class="admin-note">
            Personel hesabıyla devam edecekseniz <a href="/workspace/login"><strong>Çalışma Alanı</strong></a> girişini kullanın.
            Şifrenizi unuttuysanız <a href="/auth/forgot-password"><strong>şifre yenileme</strong></a> akışını başlatın.
        </div>
    </form>
</section>
